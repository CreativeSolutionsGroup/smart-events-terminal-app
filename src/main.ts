// Heartbeat pulse work stuff thing

// imported stuff, not sure which ones are really necessary
let https = require('https');
let fs = require('fs');
let WebSocketServer = require('websocket').server;

